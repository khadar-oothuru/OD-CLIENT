// import api from './api';

// export const sendOtp = async (phoneNumber) => {
//   const response = await api.post('/otp/send', { phoneNumber });
//   return response.data;
// };

// export const verifyOtp = async (otp, phoneNumber) => {
//   const response = await api.post('/otp/verify', { otp, phoneNumber });
//   return response.data;
// };
